import {useQuery} from 'react-query';

export const searchLaboratory = ({text}: {text: string}) => {
  // const {data, isLoading} = useQuery('laboratories', getLaboratories);
  // console.log(data, isLoading);
};
